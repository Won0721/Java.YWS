package practice;

public class SetEx {
	/* 요소들을 집합적으로 모아놓은 자료구조
	 * 
	 * 중복을 허용하지않음
	 * 저장 순서를 유지하지 않음
	 * 
	 * Set을 구현한 클래스 : HashSet, TreeSet
	 * 
	 * 1)HashSet 
	 * 	: 중복이 없고, 순서가 없다
	 *	
	 * 2)TreeSet  
	 * 	: 중복이 없으나, 정렬기능이 추가된 형태
	 * 기준 데이터보다 작다면 기준의 왼쪽에, 크다면 오른쪽에 위치
	 * => 이진 탐색 트리의 구조
	 */
	
}
